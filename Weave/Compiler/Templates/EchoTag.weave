@model EchoTag

{{if model.Encoded}}
    writer.Write(encode(
        {{@RenderCode model.Expression}}
        ));
{{else}}
    writer.Write(
        {{@RenderCode model.Expression}}
        );
{{/if}}