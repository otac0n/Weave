@namespace Weave.Compiler
@model EchoTag
@using Weave.Expressions

{{if model.Encoded}}
    writer.Write(encode(
        {{@RenderCode model.Expression}}
        ));
{{else}}
    writer.Write(
        {{@RenderCode model.Expression}}
        );
{{/if}}