@namespace Weave.Compiler
@methodname RenderBranch
@model Branch
@using Weave.Expressions

{{if model.Expression != null}}
    if ({{ this.RenderCode(model.Expression, writer); }})
{{/if}}
{
    {{@WalkElements model.Body}}
}
