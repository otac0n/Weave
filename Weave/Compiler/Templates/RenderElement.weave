@namespace Weave.Compiler
@methodname RenderRenderElement
@model RenderElement
@using Weave.Expressions

{{ var newModel = this.CreateVariable("model"); }}
var {{= newModel }} = {{@RenderCode model.Expression}};
{{@RenderCode model.Method}}({{= newModel }}, writer, indentation{{if !string.IsNullOrEmpty(model.Indentation)}} + {{= ToLiteral(model.Indentation) }}{{/if}});
