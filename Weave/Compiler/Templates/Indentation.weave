@namespace Weave.Compiler
@methodname UpdateIndentation
@model Element
@encode ToLiteral
@using Weave.Expressions

{{
    string indent;
    this.indentation.TryGetValue(model, out indent);
}}
{{if indent != null}}
    indentation = originalIndentation{{if indent.Length > 0}} + {{: indent }}{{/if}};
{{/if}}
