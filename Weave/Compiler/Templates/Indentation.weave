@methodname UpdateIndentation
@model Element

{{
    string indent;
    this.indentation.TryGetValue(model, out indent);
}}
{{if indent != null}}
    indentation = __originalIndentation{{if indent.Length > 0}} + {{: indent }}{{/if}};
{{/if}}
